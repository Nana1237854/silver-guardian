# 银发守护者 (Silver Guardian)

面向老年人的智能陪伴系统，提供健康管理、用药提醒、AI 对话、亲情相册等功能。
Android 原生应用，SQLite 本地存储，智谱 GLM-4 驱动 AI 对话，高德 SDK 驱动地图与导航。

## Language

**老人 (Elder)**:
应用的主要使用者，通过 PIN 码登录。每个老人拥有完全独立的数据上下文。
_Avoid_: 用户、patient

**家属 (Family Member)**:
与老人关联的子女或其他亲属，可在子女模式中查看老人数据并远程管理。
_Avoid_: 监护人、子女

**子女模式 (Child Mode)**:
家属专用的监控与管理面板，可查看老人健康/用药/相册数据，远程添加用药提醒、查看老人位置。
_Avoid_: 家属端、监控模式

**健康档案 (Health Record)**:
老人的健康指标记录，支持 13 种指标类型（血压、心率、血氧、睡眠、步数、血糖、体温、体重、体脂率、呼吸率、运动、心情、呼吸正念）。支持手动记录和蓝牙设备同步。
_Avoid_: 健康数据、体检记录

**今日健康概览 (Today's Health Overview)**:
健康档案首页的摘要视图，展示当天最新 4 项核心指标（血压、心率、血氧、睡眠）及 AI 状态评估。数据优先外部实时来源（蓝牙/AI），落库后从 SQLite 读取。
_Avoid_: 健康摘要、概览卡片

**用药提醒 (Medicine Reminder)**:
按时间计划提醒老人服药的机制，包含药品信息、服用时间、打卡记录。触发时通过通知栏 + TTS 语音播报。错过未打卡可按配置重复提醒。
_Avoid_: 吃药提醒、闹钟

**服药打卡 (Medicine Check-in)**:
老人确认已服药的每日签到记录，关联具体药品和日期。
_Avoid_: 签到、确认

**药品库 (Medicine Library)**:
可供搜索和浏览的药品参考目录，按疾病分类，包含通用名、品牌名、类型和说明。每个老人拥有一份独立拷贝（从默认模板初始化），AI 推荐的新药品可反向扩充入库。老人可浏览，家属可管理（CRUD）。
_Avoid_: 药品目录、药典

**智能对话 (AI Chat)**:
通过智谱 GLM-4 模型驱动的对话功能。支持语音输入（长按说话）和 AI 回复自动语音播报。AI 推荐药物时输出结构化 JSON，App 解析后弹窗确认入库。对话时向 AI 提供当前用户用药列表和服药打卡状态作为上下文。
_Avoid_: 聊天、消息

**亲情相册 (Family Album)**:
家属为老人上传的照片集合，支持拍照和相册选取、单张删除和整相册删除。图片双存于 App 私有目录和外部 Pictures 文件夹。按分类（category）分组展示。
_Avoid_: 照片墙、图库

**一键呼叫 (One-Tap Call)**:
首页快捷按钮，点击弹出大按钮快捷选择家属（不含 120），选人后直接拨出电话。需 CALL_PHONE 权限。
_Avoid_: 直接拨号、快捷拨号

**SOS 紧急呼叫 (SOS Emergency)**:
首页红色紧急按钮。点击弹出确认对话框"是否紧急呼叫？"，10 秒未操作自动拨打 120（课设展示使用空号）。确认后弹出联系人列表（120 + 家属），支持逐个拨打直到有人接听。
_Avoid_: 报警、求救

**便民查询 (Community POI Search)**:
基于高德地图 SDK，以老人当前位置搜索附近菜市场、医院、药店、超市、银行。点击 POI 在地图上绘制步行路线，信息栏显示距离和时间，支持跳转高德 App 导航（含网页版兜底）。
_Avoid_: 周边查询、地图搜索

**防诈骗推送 (Fraud Prevention Push)**:
远程拉取反电信诈骗知识并每日推送展示，含案例和防范措施。
_Avoid_: 安全提示、反诈

**语音播报 (TTS Voice Broadcast)**:
用药提醒和健康异常告警触发时，通知栏推送的同时通过系统 TTS 引擎朗读提醒内容。
_Avoid_: 语音提醒、TTS

**长辈模式 (Elder Mode)**:
与字体设置联动的首页极简模式。通过设置页字体档位切换：标准=完整布局，加大=精简快捷网格+用药提醒卡片，特大=仅保留 SOS、一键呼叫、用药提醒、AI 对话四个大按钮。基于现有 FontScaleHelper 扩展。
_Avoid_: 大字模式、关怀模式

**每日关怀 (Daily Care Notification)**:
每天早 8 点推送的关怀通知，包含当日天气摘要和今日服药计划汇总，复用现有 ReminderBroadcastReceiver 架构。
_Avoid_: 早安推送、每日问候

**天气出行建议 (Weather & Outing Advice)**:
首页天气卡片，对接免费天气 API，结合便民查询 POI 数据给出出行建议（如"今天晴天，适合去附近公园散步"）。
_Avoid_: 天气预报、出行提示

**PIN 提示 (PIN Hint)**:
老人在创建时可填写的提示问题和答案。登录页支持 PIN 输入框显示/隐藏切换，以及"忘记 PIN"→ 验证答案 → 显示 PIN 的流程。
_Avoid_: 密码提示、忘记密码

**多用户数据隔离 (Multi-User Data Isolation)**:
每个老人拥有完全独立的数据集（健康档案、用药、相册、记忆、家属、紧急记录、药品库、防骗贴士）。新建用户时从默认模板（8 种常见药品 + 4 条防骗贴士）拷贝初始数据。所有 UI 文本（问候语、头像描述等）从当前登录用户的 name 字段动态取值，不硬编码。
_Avoid_: 多租户、数据分离

**默认模板 (Default Template)**:
新建用户时自动拷贝的初始数据集，包含 8 种常见老年病药品（硝苯地平、缬沙坦、二甲双胍、阿司匹林、碳酸钙 D3、对乙酰氨基酚、氨溴索、奥美拉唑）和 4 条防骗贴士。
_Avoid_: 初始数据、种子数据

## Relationships

- 一个 **老人** 拥有一条或多条 **健康档案** 记录
- 一个 **老人** 拥有一份独立的 **药品库** 拷贝
- 一个 **老人** 拥有一个或多个 **用药提醒**，**用药提醒** 对应多条 **服药打卡** 记录
- 一个 **老人** 关联一个或多个 **家属**
- 一个 **亲情相册** 属于一个 **老人**，由 **家属** 上传
- **智能对话** 按 **老人** 隔离历史记录，AI 可读取当前老人用药列表和打卡状态
- **一键呼叫** 拨打 **家属** 号码（不含 120）
- **SOS 紧急呼叫** 可拨打 120 和 **家属** 号码，支持逐个拨打
- **用药提醒** 触发 **语音播报**（通知栏 + TTS）
- **便民查询** 基于高德 SDK 展示老人当前位置附近的 POI，支持步行路线绘制和高德导航跳转
- **长辈模式** 通过 **字体设置** 联动触发，控制首页布局复杂度
- **每日关怀** 每天定时触发，整合天气和用药信息
- 所有数据按 **老人** 完全隔离，切换用户等于切换全部数据上下文

## Example dialogue

> **Dev:** "老人切换用户后，用药提醒是共享的还是每个老人独立的？"
> **Domain expert:** "每个老人独立的——用药计划、健康档案、相册、药品库全部按 userId 隔离。新建用户从默认模板拷贝初始数据。切换用户等于切换全部数据上下文。"

> **Dev:** "家属上传照片后，老人能看到吗？"
> **Domain expert:** "能——亲情相册是老人和家属共享的，老人端可以在相册 tab 看到所有照片。"

> **Dev:** "一键呼叫和 SOS 的区别是什么？"
> **Domain expert:** "一键呼叫弹出大按钮快捷选择家属直接拨号（快但范围小），SOS 弹出确认对话框+10秒超时自动拨打+逐个拨打家属和 120（安全但步骤多）。两者互补。"

> **Dev:** "AI 推荐的药怎么进入老人的用药列表？"
> **Domain expert:** "AI 输出 JSON 格式的药品推荐 → App 解析后弹窗确认 → 用户确认后同时写入该老人的药品库和用药提醒。药品库里没有的药自动扩充。"

> **Dev:** "标准/加大/特大字体和长辈模式是什么关系？"
> **Domain expert:** "就是同一套切换——字体选'标准'=完整首页布局，选'加大'=精简版，选'特大'=极简 4 按钮版。不额外新增设置入口。"

## Flagged ambiguities

- "用户" 既被用来指 **老人** 也指 **家属** — 已解决：登录用户 = 老人，家属不登录 App。
- "用药提醒" 的 "提醒" 在旧版中既指提醒计划又指弹窗通知 — 已解决：计划 = Medicine Reminder，弹窗 = Notification。
- "紧急呼叫" 在旧版中指 SOS 弹窗，新版课程要求指直接拨号 — 已解决：一键呼叫 = 选家属直拨，SOS = 确认→逐个拨打（含 120）。
- "120 号码" 用于课设展示 — 已解决：SOS 拨打目标中的 120 替换为空号/测试号，展示逻辑但不实际呼叫急救。
