<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_page"
    android:clipToPadding="false"
    android:fillViewport="true"
    android:overScrollMode="never">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="@dimen/screen_padding"
        android:paddingTop="@dimen/screen_padding"
        android:paddingEnd="@dimen/screen_padding"
        android:paddingBottom="@dimen/bottom_nav_safe_space">

        <include
            android:id="@+id/page_header_root"
            layout="@layout/view_page_header" />

        <include
            android:id="@+id/settings_profile_card"
            layout="@layout/view_settings_profile_card" />

        <include
            android:id="@+id/settings_family_section"
            layout="@layout/view_group_section" />

        <include
            android:id="@+id/settings_safety_section"
            layout="@layout/view_group_section" />

        <include
            android:id="@+id/settings_display_section"
            layout="@layout/view_group_section" />

        <include
            android:id="@+id/settings_other_section"
            layout="@layout/view_group_section" />
    </LinearLayout>
</ScrollView>